uploadArchives {
    repositories.mavenDeployer {
        repository(url: "file:./repo/releases") {
            authentication(userName: "", password: "")
        }

        snapshotRepository(url: "file:./repo/snapshots") {
            authentication(userName: "", password: "")
        }
    }
}

